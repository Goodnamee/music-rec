# Actual versions from deeplearning env (2026-06-16)
# Install: pip install -r requirements.txt
# For GPU: ensure torch matches your CUDA version (>=12.8 for Blackwell/Blackwell Pro)

# Core ML
torch>=2.6.0
transformers>=5.8.0
accelerate>=1.13.0
datasets>=4.8.0
peft>=0.19.0
bitsandbytes>=0.49.0

# Tokenization
sentencepiece>=0.2.0
tokenizers>=0.22.0
safetensors>=0.7.0

# SID construction
scikit-learn>=1.8.0
numpy>=2.0

# Data
pandas>=3.0.0
tqdm>=4.67.0
fire>=0.7.0

# Optional (not required for core pipeline)
# faiss-cpu>=1.13    # faster KMeans via faiss
# flash-attn         # Flash Attention 2 (Linux only, 2-3x training speedup)

# === Qwen3 model ===
# Downloaded automatically by transformers on first use (~432MB, no auth).
# python -c "from transformers import AutoModelForCausalLM; AutoModelForCausalLM.from_pretrained('Qwen/Qwen3-0.6B')"
# Pre-download to bypass slow CDN: export HF_ENDPOINT=https://hf-mirror.com (China mirror)
